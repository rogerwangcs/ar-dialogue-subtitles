const personTalking = (faces, left) => {
    const array = []
    if (faces)
    finalX = faces[0]
    finalY = faces[1]
    for (){
        if (left){

        } else {

        }
    }
    array[0] = finalX
    array[1] = finalY
    //returns the x and y coordinates of the talking person.
    //Left is a boolean. If it's true it means the leftmost person is talking. If false it means the right most person is talking
    return array
    //completely useless because
}
